# ----- LLM provider (OpenAI) -----
# Get a key from https://platform.openai.com/api-keys
# NOTE: this must be an API key with usable *API* credit (not Codex-CLI-only).
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
LLM_MODEL=gpt-4o-mini
LLM_MAX_TOKENS=4096
# Optional: override the base URL (e.g. Azure/OpenAI-compatible gateways).
# OPENAI_BASE_URL=https://api.openai.com/v1

# ----- Weather (Open-Meteo is keyless; no key needed) -----
OPEN_METEO_FORECAST_URL=https://api.open-meteo.com/v1/forecast
OPEN_METEO_GEOCODE_URL=https://geocoding-api.open-meteo.com/v1/search

# ----- Storage -----
DATABASE_URL=sqlite:///./data/agrisense.db
CHROMA_DIR=./data/chroma
KB_DIR=./data/knowledge_base

# ----- bdapps CaaS (sandbox) -----
# Leave blank to run the local simulator. Fill to hit the real sandbox.
BDAPPS_APP_ID=
BDAPPS_APP_PASSWORD=
BDAPPS_CAAS_URL=https://developer.bdapps.com/cass/send
BDAPPS_SANDBOX=true

# ----- App -----
APP_ENV=development
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
